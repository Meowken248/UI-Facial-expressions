import { useCallback, useEffect, useRef, useState } from 'react';import { EmotionScreen } from '../components/dashboard/EmotionScreen';import { EmotionControls } from '../components/controls/EmotionControls';import { useEmotionController } from '../hooks/useEmotionController';
export default function App(){const controller=useEmotionController();const [autoBlink,setAutoBlink]=useState(true);const [followPointer,setFollowPointer]=useState(true);const [animated,setAnimated]=useState(true);const screenRef=useRef<HTMLDivElement>(null);const fullscreen=useCallback(async()=>{if(!document.fullscreenElement&&screenRef.current?.requestFullscreen)await screenRef.current.requestFullscreen()},[]);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(['INPUT','TEXTAREA'].includes((e.target as HTMLElement).tagName))return;if(e.key==='ArrowLeft')controller.previous();if(e.key==='ArrowRight')controller.next();if(e.code==='Space'){e.preventDefault();controller.setAutoPlay(!controller.autoPlay)}if(e.key.toLowerCase()==='f')void fullscreen()};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[controller,fullscreen]);return <main><div className="stage" ref={screenRef}><div className="device-frame"><EmotionScreen emotion={controller.emotion} autoBlink={autoBlink} followPointer={followPointer} animated={animated}/></div></div><EmotionControls emotion={controller.emotion} onSelect={controller.setEmotion} onPrevious={controller.previous} onNext={controller.next} autoBlink={autoBlink} setAutoBlink={setAutoBlink} followPointer={followPointer} setFollowPointer={setFollowPointer} animated={animated} setAnimated={setAnimated} autoPlay={controller.autoPlay} setAutoPlay={controller.setAutoPlay} onFullscreen={()=>void fullscreen()}/></main>}
