import { emotionLabels, emotionOrder } from '../../config/emotion.labels';import type { Emotion } from '../../types/emotion.types';
interface Props{emotion:Emotion;onSelect:(e:Emotion)=>void;onPrevious:()=>void;onNext:()=>void;autoBlink:boolean;setAutoBlink:(v:boolean)=>void;followPointer:boolean;setFollowPointer:(v:boolean)=>void;animated:boolean;setAnimated:(v:boolean)=>void;autoPlay:boolean;setAutoPlay:(v:boolean)=>void;onFullscreen:()=>void}
function Toggle({label,pressed,onClick}:{label:string;pressed:boolean;onClick:()=>void}){return <button className={`toggle ${pressed?'on':''}`} aria-pressed={pressed} onClick={onClick}><i/><span>{label}</span></button>}
export function EmotionControls(p:Props){return <section className="controls" aria-label="Điều khiển biểu cảm"><div className="control-heading"><div><span>Thư viện biểu cảm</span><h1>{emotionLabels[p.emotion]}</h1></div><div className="stepper"><button onClick={p.onPrevious} aria-label="Biểu cảm trước">← Trước</button><button onClick={p.onNext} aria-label="Biểu cảm tiếp theo">Sau →</button></div></div><div className="emotion-grid">{emotionOrder.map((e,i)=><button key={e} className={e===p.emotion?'selected':''} aria-pressed={e===p.emotion} onClick={()=>p.onSelect(e)}><b>{String(i+1).padStart(2,'0')}</b><span>{emotionLabels[e]}</span></button>)}</div><div className="toggles"><Toggle label="Chớp mắt" pressed={p.autoBlink} onClick={()=>p.setAutoBlink(!p.autoBlink)}/><Toggle label="Theo con trỏ" pressed={p.followPointer} onClick={()=>p.setFollowPointer(!p.followPointer)}/><Toggle label="Chuyển động" pressed={p.animated} onClick={()=>p.setAnimated(!p.animated)}/><Toggle label="Tự động phát" pressed={p.autoPlay} onClick={()=>p.setAutoPlay(!p.autoPlay)}/><button className="fullscreen" onClick={p.onFullscreen}>Toàn màn hình</button></div></section>}
